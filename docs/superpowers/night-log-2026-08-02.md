# Night Log — 2026-08-02

Work branch: `night/2026-08-02` (from `dev`).
Loop state: `.claude/ralph-loop.local.md` (max 40 iterations, completion promise set).

## Baseline (measured at setup, before any change)

- `.venv/bin/pytest` — **223 passed in 2.19s**, all green.
- `.venv/bin/ruff check .` — **1156 errors**, pre-existing on `dev`. Breakdown:
  861 W293 (blank line with whitespace), 90 I001 (unsorted imports), 78 F401
  (unused import), 39 W291 (trailing whitespace), 36 E701, 17 F841, 13 F541,
  11 E722 (bare except), 8 E402, 3 misc. 896 auto-fixable.
  → The loop must NOT mass-fix these. Gate is: no new errors, touched files clean.
- Dev extras were missing from `.venv`; installed at setup
  (`pip install -e ".[dev]"` → ruff 0.16.1, mypy 2.3.0, pytest 9.0.2).

## Setup findings (before iteration 1)

**`arcade_scanner/database/json_store.py` is dead code.** Nothing imports it —
the only remaining mentions are docstring references in `sqlite_store.py`
("Drop-in replacement for JSONStore"). CLAUDE.md still claims it is "the legacy
format migrated at startup", which is no longer true. This matters for triage:
three of the six orphan branches only touch this file.

**Three branches were abandoned by their own author**, per their tip commits:
`fix-json-store-concurrency` ("Abandon PR due to merge conflicts"),
`perf-duplicate-detector-bktree` ("Stop work on obsolete PR"),
`perf-optimize-db-save` ("Acknowledge PR closure").

**`refactor-state-management` is superseded.** It adds `static/state.js`; `dev`
ships `static/store.js` instead. Merge 759e260 resolved exactly this conflict
"in favor of dev".

## Morning decisions queued for the user

- [ ] **`.venv` has two interpreters and `.venv/bin/pytest` runs the one without
      imagehash/Pillow** — the documented test command silently skips every
      image-dedup test. See iteration 2. Decide how to unify, then fix CLAUDE.md.
- [ ] Delete the orphan remote branches that the triage rules out (the loop only
      proposes; it never deletes).
- [ ] Whether to run a repo-wide `ruff check --fix` for the ~900 whitespace
      errors as a separate isolated commit. Not done overnight — it would bury
      the night's real diff in noise.
- [ ] **Decide how `SQLiteStore` shares its connection** — media-path *reads* are
      still unlocked and can hit `sqlite3.InterfaceError` against a concurrent
      write. Serialise all reads, or per-thread connections (watch the fd count
      under `ThreadingTCPServer`). See iteration 3, "Left deliberately unfixed".
- [ ] Fix the stale `json_store.py` claim in CLAUDE.md, and decide whether to
      delete the dead module. Also `SQLiteStore`'s docstring claims "per-thread
      connections", which it does not have.

---

## Phase A — Branch triage

| Branch | Verdict | Iteration |
|---|---|---|
| `perf/optimize-fs-walker` | **DROP** (measured, propose delete) | 1 |
| `perf-duplicate-detector-bktree` | **DROP** the code, but it exposed a real bug — fixed | 2 |
| `fix-json-store-concurrency` | **DROP** (net diff is whitespace) — but its concern was real in live code | 3 |
| `perf-optimize-db-save` | **DROP** (optimises a no-op) | 4 |
| `perf-optimize-scanner-save` | **DROP** (calls a method the live store lacks) | 4 |
| `refactor-state-management` | **DROP** (dev shipped the same refactor) | 5 |

## Phase B — Coverage sweep

| Module | LOC | Status | Iteration |
|---|---|---|---|
| `core/duplicate_detector.py` | 684 | **done** — 7 tests, image near-miss path | 2 |
| `core/bitrate_analyzer.py` | 528 | **done** — 69 tests | 6 |
| `scanner/file_system.py` | 176 | **done** — 55 tests, thread-leak fixed | 7 |
| `scanner/media_probe.py` | 157 | **done** — 34 tests, 2 bugs fixed | 8 |
| `scanner/video_inspector.py` | 65 | **done** — 33 tests, no bugs | 9 |
| `scanner/manager.py` | — | pending | — |
| `server/routes/files.py` | 694 | pending | — |
| `server/routes/queue.py` | 565 | pending | — |

---

## Iteration log

<!-- Each iteration appends one section here: what was done, what was measured,
     what was decided, and whether the suite ended green. -->

### Iteration 1 — `perf/optimize-fs-walker-17365163716527261640` → DROP

Commit `570a230` "⚡ Optimize file system traversal using os.scandir", 1 file,
+60/-15 in `arcade_scanner/scanner/file_system.py`.

**Cherry-pick onto the work branch conflicts** (`UU file_system.py`, aborted
cleanly). The branch was written against a much older `file_system.py`; `dev`
has since rewritten `_walker_worker` substantially.

**Applying it would regress three things that landed on dev afterwards:**

1. *Queue protocol.* The branch does `queue.put_nowait(entry.path)` — a bare
   string. `scanner/manager.py:249` unpacks `async for file_path, dir_changed in
   fs_scanner.scan_directories(...)`. Bare paths raise `ValueError` on the first
   file: the scanner would be dead on arrival.
2. *Backpressure.* Dev deliberately uses a bounded `Queue(maxsize=500)` with a
   blocking `run_coroutine_threadsafe(queue.put(...)).result()` — the comment
   calls it "natural backpressure". The branch's `call_soon_threadsafe(
   queue.put_nowait, ...)` is non-blocking and would raise `QueueFull` past 500
   queued paths, exactly the memory-explosion case the bound exists to prevent.
3. *Incremental scan.* Dev skips directories whose `mtime` predates
   `_last_scan_time` and reports `⚡ Skipped N unchanged directories`. The
   branch has no notion of this and would rescan everything every time.

**And the payoff does not justify re-implementing the idea on top of dev.**
Benchmarked both traversal strategies with identical filter semantics on a
synthetic tree (300 dirs, 12 000 files, 2 exclusions, warm cache, median of 7):

```
os.walk    median  21.0 ms   best  20.4 ms   2630 hits
os.scandir median  19.0 ms   best  18.9 ms   2630 hits
→ scandir +9.7% faster; identical result sets
```

Real but irrelevant: ~2 ms saved per 12 000 files. A scan's cost is dominated by
one `ffprobe` subprocess per *new* file (tens to hundreds of ms each), and the
incremental-scan skip that dev already has is the far larger lever. Optimizing
the walker trades a guaranteed regression for noise-level gains.

**Proposed for the morning:** delete `origin/perf/optimize-fs-walker-17365163716527261640`
(the loop does not delete branches itself). Benchmark script kept at
`docs/superpowers/bench/bench_walker.py` so the measurement is reproducible.

Suite after iteration: **223 passed**, no production code touched.

### Iteration 2 — `perf-duplicate-detector-bktree` → DROP the branch, but it found a real bug

**⚠️ Read this section first in the morning — it contains the night's most
important finding, and it is not about the branch.**

#### The branch itself: superseded

Commit `202bf8d` adds a BK-tree to replace an O(n²) all-pairs loop in
`_find_image_duplicates_by_hash`. That loop no longer exists — `dev` has since
replaced it with exact-hash bucketing plus a prefix-bucket near-miss pass. The
branch's tip commit is "Stop work on obsolete PR". Nothing to merge.

#### But the thing that replaced it is broken

The prefix-bucket pass grouped hashes by their first 4 hex chars and only
compared *within* a bucket, justified by this comment:

> images with different prefixes have hash distance > 16 bits, so can't match at
> threshold ≤ 5

That reasoning is wrong. A 4-char prefix bucket holds hashes whose leading 16
bits are *identical*. Two hashes differing by a single bit inside those leading
16 bits land in different buckets and are never compared — despite a Hamming
distance of 1, far below the threshold of 5.

Confirmed by a characterization test before touching anything: two hashes one
bit apart in the *last* bit are grouped, the same two one bit apart in the
*first* bit are not. Same distance, opposite outcome.

Measured against brute force as ground truth (5000 hashes, threshold 5):

```
      n      brute     prefix     banded    prefix recall  banded recall
    500      95.9ms       0.1ms       2.4ms            43.6%         100.0%
   2000    1566.6ms       0.6ms      18.3ms            40.2%         100.0%
   5000    9871.6ms       1.7ms      85.2ms            43.8%         100.0%
```

**The shipped duplicate finder was missing roughly 56% of near-duplicate image
pairs.** Exact duplicates were always found (that is a separate O(n) pass) — the
loss is confined to near-misses, i.e. re-encodes, resizes, and re-saves, which
is precisely what perceptual hashing exists to catch.

#### The fix

Replaced prefix bucketing with a banded index (`_BandedHashIndex`): each hash is
split into `threshold + 1` bands and indexed per band. A pair within the
threshold differs in at most `threshold` bits, which can touch at most
`threshold` bands, so at least one band survives intact — a lookup across a
hash's own bands returns every true match. Pigeonhole principle; no false
negatives by construction, and verified empirically against brute force.

The BK-tree from the branch was also implemented and measured first. It is
exact but slow — 5202ms at n=5000, only 2× faster than brute force, because at
threshold 5 the pruning window is wide relative to how random 64-bit hashes
distribute. Banding is ~60× faster than the BK-tree at the same recall, so the
branch's data structure was discarded in favour of it.

Distance comparison switched from `imagehash.ImageHash.__sub__` to integer
popcount: identical results, 637ms → 43ms over 780 758 candidate checks, since
the operator routes through numpy. Guarded with a `bin(x).count("1")` fallback
because `pyproject.toml` declares `requires-python = ">=3.8"` while `int.bit_count`
needs 3.10. (Note the inconsistency: README says "Python 3.10+".)

Cost of correctness: that phase goes from 1.7ms to 85ms at n=5000. Worth it.

**Tests:** `tests/test_duplicate_detector.py`, 7 tests — the head/tail asymmetry
pair, exact matches, distant hashes, `threshold=0`, vanished files, and a
randomized test comparing against brute force. Verified they fail against the
old implementation (2 failed) and pass against the new (7 passed); the
randomized one independently caught a dropped pair.

**Proposed for the morning:** delete
`origin/perf-duplicate-detector-bktree-8262525754746318244` — its idea is now
implemented better, in `dev`'s lineage rather than against a two-refactors-old base.

#### Second finding: the documented test command runs a partly blind suite

`.venv` contains **two interpreters**. `.venv/bin/python3` → 3.13, but
`.venv/bin/pytest` has a `python3.14` shebang:

| | 3.13 | 3.14 |
|---|---|---|
| imagehash, Pillow | ✅ | ❌ |
| pydantic, pytest | ✅ | ✅ |

`duplicate_detector.py` degrades gracefully when imagehash is missing
(`IMAGEHASH_AVAILABLE = False`), so under 3.14 every image-hashing path is
simply absent — and the new tests **skipped silently** rather than failing.

```
.venv/bin/pytest              → 223 passed, 7 skipped   ← the command in CLAUDE.md
.venv/bin/python3.13 -m pytest → 230 passed             ← everything actually runs
```

The recall bug lived in code that the documented test command cannot even
import. Both are green now, but the blind spot is the more dangerous of the two
findings: the suite reports success while never touching image dedup.

**Proposed for the morning:** either install imagehash/Pillow into 3.14, or
repoint `.venv/bin/pytest` at 3.13, or rebuild the venv on one interpreter — then
update the commands in CLAUDE.md. Not done here: rebuilding the user's venv is
their call, and a wrong guess would break the app rather than a test.

Benchmark: `docs/superpowers/bench/bench_image_dedup.py` (run with python3.13).

Suite after iteration: **230 passed** under 3.13, **223 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1148, down from the 1156 baseline.

### Iteration 3 — `fix-json-store-concurrency` → DROP, but it pointed at a live bug

**⚠️ This iteration found a production race in the encoding queue. Read it.**

#### The branch: nothing left to merge

Two commits. `d2bca17` adds `import threading`, a `self._lock`, and five `with
self._lock:` blocks to `JSONStore`. `944b389` — "Abandon PR due to merge
conflicts" — **takes them all back out**. The net diff against the merge base is
whitespace only: five trailing-space removals in a file nothing imports.

`json_store.py` is confirmed dead. A repo-wide search for `json_store` /
`JSONStore` (py, js, json, md, sh, toml) finds only: the module defining itself,
two docstring references in `sqlite_store.py` ("Drop-in replacement for
JSONStore"), a historical note in `dev-docs/performance-roadmap.md`, and the
stale claim in CLAUDE.md that it is "the legacy format migrated at startup".

#### The concern behind it, checked against the store that actually runs

The branch worried about unsynchronised access to the store. `SQLiteStore` does
hold a `_write_lock` and does use `check_same_thread=False` — but the lock
covered only the media-table writes (`upsert`, `bulk_upsert`, `remove`,
`delete_all_photos`). **Every `encoding_queue` method ran unlocked**, including
`get_next_pending`, whose docstring reads "Atomically claim the oldest pending
job".

It was not atomic. It ran a SELECT and then a separate UPDATE — on a connection
opened with `isolation_level=None`, i.e. autocommit, so the two statements are
independent transactions with nothing held between them. The UPDATE did carry
the right compare-and-swap guard (`WHERE id = ? AND status = 'pending'`), but
**the return value ignored whether the swap succeeded**: a caller that lost the
race still returned the job dict to its worker.

Measured before changing anything — 8 threads on a `threading.Barrier`, one
pending job, 40 rounds:

```
38/40 rounds handed one job to multiple workers, e.g. (0, [1, 1, 1, None, 1, 1, 1])
draining a 60-job queue from 8 threads: jobs claimed more than once: [1, 3, 5, 52, 58]
plus repeated sqlite3.InterfaceError: bad parameter or other API misuse
```

Six workers received the same job in a single round. The `InterfaceError` is
separate and structural: Python's `sqlite3` is threadsafety level 1, so one
connection shared across threads must be serialised externally or it raises.

This is not theoretical. `scripts/mac_worker.py` polls this queue over HTTP and
`scripts/batch_controller.py` runs parallel encodes — two workers claiming one
job means the same file encoded twice, both writing the same `_opt.` output.

#### The fix

`get_next_pending` now holds the lock across the claim and honours
`cursor.rowcount`: losing the compare-and-swap means looping to the next
pending job, never returning one the caller does not own. The lock was extended
to the rest of the `encoding_queue` surface — `queue_encode` (its check-then-
insert had the same shape of race, allowing duplicate pending jobs for one
file), `update_job_status`, `get_queue_status`, `cancel_job`, `is_job_cancelled`,
`cleanup_old_jobs`.

Changed to `threading.RLock`: `_notify_change` fires *inside* the lock in
`upsert`, so once reads are locked too, any callback re-entering the store would
deadlock on a plain `Lock`. Today the only callback is `_media_cache.invalidate`
(takes its own lock, never touches the store), so this is insurance rather than
a live fix — but the existing `test_write_lock_exists` asserted
`isinstance(..., threading.Lock)` and had to be updated; it now checks
reentrancy behaviourally rather than by type.

**Tests:** 4 added to `tests/test_sqlite_store.py` — barrier-synchronised
single-job contention, concurrent drain of a full queue, claim marks the row
`downloading` and a second caller gets `None`, and duplicate-file queueing is
rejected. Verified failing before the fix (2 failed with the numbers above) and
passing after, re-run 5× for flakiness.

#### Left deliberately unfixed

Reads on the **media** path (`get_all`, `get_page`, `count`, `get`, …) still
touch the shared connection without the lock, so the same `InterfaceError` is
reachable when a dashboard read overlaps a write. Fixing it properly is a design
choice, not a patch: serialise every read through the one connection (simple,
but the dashboard's full-table reads then queue behind writes), or give each
thread its own connection as the class docstring already wrongly claims
("Thread-safe via per-thread connections + write lock"). The second option is
tempting but risky here — `ThreadingTCPServer` spawns a thread per request, so
per-thread connections would open one file handle per request, and this project
has already been bitten once by "Too many open files" (CHANGELOG 6.4.1).
That trade-off wants a waking decision, so it is queued below rather than
guessed at 3am.

**Proposed for the morning:** delete
`origin/fix-json-store-concurrency-8952611763497830264`.

Suite after iteration: **234 passed** under 3.13, **227 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1143, below the 1156 baseline.

### Iteration 4 — `perf-optimize-db-save` and `perf-optimize-scanner-save` → both DROP

Triaged together: they are one idea split across two branches (debounce or
offload the database save), and the same single fact kills both.

**`SQLiteStore.save()` is a no-op.** In full:

```python
def save(self) -> None:
    """No-op — SQLite auto-commits on every write."""
    pass
```

Both branches optimise the cost of saving. That cost was real for `JSONStore`,
which rewrote the entire JSON file on every call. The SQLite migration removed
it — writes commit inside `upsert`/`bulk_upsert`, and `save()` survives only as
a compatibility shim. There is nothing left to debounce or offload.

**`perf-optimize-db-save`** (`6b18f52`, tip "Acknowledge PR closure") adds
`schedule_save()` to `JSONStore` (dead module) and swaps four `db.save()` calls
in `api_handler.py` for it. Cherry-pick conflicts, and the target calls are
gone regardless: `api_handler.py` now contains **zero** `db.save()` calls — they
moved into `server/routes/` when the routes were extracted.

**`perf-optimize-scanner-save`** (`b680b48`) rewrites a periodic save in the
scanner loop as:

```python
snapshot = db.get_data_snapshot()
await asyncio.to_thread(db.save, snapshot)
```

Three problems. `get_data_snapshot` exists only on `JSONStore`; `SQLiteStore`
has no such method, so this raises `AttributeError` against the live store.
`db.save` takes no argument there, so the call signature is wrong too. And the
loop it patches — `if processed_count % 500 == 0 ... if current_time -
last_save_time > 60` — no longer exists: `scanner/manager.py` has no `db.save()`
call at all any more. Cherry-pick duly conflicts.

Nothing measured this iteration: benchmarking a debounce of `pass` would be
theatre. The verdict rests on reading the code, and the code is unambiguous.

**Noted in passing:** five `db.save()` calls remain in live code
(`routes/duplicates.py` ×2, `routes/files.py` ×3). All are no-ops. Harmless, but
they read as if they persist something, which is how someone ends up writing a
branch to optimise them. Worth deleting during a future cleanup — not tonight,
since it touches request-handling paths for zero behavioural gain.

**Proposed for the morning:** delete
`origin/perf-optimize-db-save-16961943653823358906` and
`origin/perf-optimize-scanner-save-9781626674378577018`.

No code changed. Suite unchanged: **234 passed** under 3.13.

### Iteration 5 — `refactor-state-management` → DROP. **Phase A complete.**

Commit `f1242a7` moves the frontend's grouped globals out of `engine.js` into a
new `static/state.js` and registers it in the dashboard template.

`dev` already did this, with a different file. `static/store.js` (143 lines) is a
Redux-lite `Store` class with `getState`/`subscribe`/`dispatch` and a reducer,
exposed as `window.AppState`, plus `mapGlobalToState()` which re-attaches the
old global names as `Object.defineProperty` accessors. The blocks the branch
wants to remove from `engine.js` are already gone — what stands there now is a
comment pointing at `store.js`. Cherry-pick conflicts on both `engine.js` and
`dashboard_template.py`. Merge 759e260 ("resolve state-refactor conflicts in
favor of dev") already made this call; this only confirms it.

**Checked the replacement rather than assuming it works**, since a globals-to-
accessors migration fails in specific, quiet ways:

- *Load order.* `store.js` is the first script tag (template line 198), ahead of
  all 20 others, so the accessors exist before anything reads them.
- *Lexical shadowing.* A top-level `let`/`const` of a mapped name in any later
  file would shadow the `window` accessor, and reads would silently bypass the
  store. Searched every file in `static/` for top-level redeclarations of the
  eight mapped globals: none. (`git log` shows this exact class of bug was hit
  twice before — `6fdfab1` and `625c08d`.)
- *`availableTags` mapping.* It dispatches `SET_DB_STATS`, whose reducer accepts
  both a bare array and `{tags: [...]}`, so both call shapes land in
  `data.availableTags`. This was `625c08d`'s fix and it holds.

Frontend contract tests pass: 46 across `test_js_syntax`, `test_dom_contract`,
`test_js_completeness`, `test_js_runtime_patterns`.

One stale comment noticed, not worth a commit on its own: `utils.js:155` still
says "activeTags, filterUntaggedOnly, minSizeMB, maxSizeMB, dateFilter,
availableTags are declared at the top of the file" — they are not; they moved to
`store.js`. Worth deleting whenever that file is next touched.

**Proposed for the morning:** delete
`origin/refactor-state-management-14222868568131144284`.

No code changed. Suite: **234 passed** under 3.13.

---

**Phase A is finished: all six branches have verdicts, all six are DROP.** Not
one carried mergeable code — three were abandoned by their author, and the other
three were overtaken by later work on `dev`. The value of the exercise was
elsewhere: two of them pointed at real defects in the code that had replaced
them (image-dedup recall, encoding-queue claim race), both now fixed and tested.
Phase B continues with `core/bitrate_analyzer.py`.

### Iteration 6 — Phase B: `core/bitrate_analyzer.py` → 69 tests

`analyze_bitrate()` shells out to ffprobe three times (stream info, per-packet
data, audio). The tests swap in a `subprocess.run` stub that answers each call
by inspecting its arguments, so the whole module is exercised without ffprobe
and without a media file.

Covered: codec/resolution/duration parsing, fractional frame rates
(`30000/1001`), the `0/0` guard, stream-bitrate-beats-format-bitrate precedence,
packet aggregation into one-second windows, min/max/variance, the
`TimeoutExpired` fallback (max = 2x average), the no-packets fallback (1.5x and
0.3x), audio detection, every entry in `CODEC_EFFICIENCY`, headroom, the VBR
branches, the never-exceed-source cap, the 200/300/500 floors, and the
per-encoder branches of `as_ffmpeg_args` including all six CRF thresholds.

**Most of this module is not used in production.** `scripts/video_optimizer.py`
imports only `analyze_bitrate` and `BitrateProfile`. `calculate_encoding_params`,
`EncodingParams.as_ffmpeg_args`, `get_encoding_params_for_file` and `main()`
have no caller outside the module's own CLI. Tested anyway — the CLI is a
documented entry point — but worth knowing before anyone invests in it.

#### Two imprecisions found, deliberately not "fixed"

*Partial trailing window.* Packets are bucketed by `int(pts_time)`, so a clip
that does not end on a second boundary leaves a short final bucket that is then
compared against full ones. Constant-bitrate content can therefore be reported
as variable: ten identical seconds plus a very short tail push the variance
ratio past 0.25 and flip `is_variable_bitrate` to True. Production reads that
flag to size the VBR buffer (`video_optimizer.py:961`), so such a clip gets a
2.0x buffer instead of 1.5x. `min_bitrate_kbps` is wrong for the same reason,
but nothing reads it.

Left alone on purpose: this is a heuristic feeding another heuristic, the
consequence is a slightly generous encoder buffer hint rather than a wrong
result, and any fix (drop the last bucket, or weight buckets by real duration)
changes encoding parameters for every real file. That is a waking decision.
Two tests pin the current behaviour so a future change is deliberate.

*Duplicate `-crf`.* `as_ffmpeg_args()` appends its own estimated `-crf` for
libx264 even when `encoder_options` already contains one — which the module's
own `main()` does, passing `["-preset", "ultrafast", "-crf", "28"]`. ffmpeg
honours the last occurrence, so the caller's value silently loses. Harmless
today (nothing in production takes this path), pinned by a test.

One test of mine was wrong on the first run and the code was right: I predicted
a 200 kbps tail would flip the VBR flag, but the variance ratio lands at 0.248,
just under the 0.25 threshold. Corrected to assert what actually holds, with a
shorter tail used to demonstrate the flip.

Suite after iteration: **303 passed** under 3.13, **296 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1143.

### Iteration 7 — Phase B: `scanner/file_system.py` → 55 tests, and a leaked thread fixed

Tests run against synthetic trees under `tmp_path` with `config` patched inside
the module's namespace. The project has no `pytest-asyncio`, so the async
generator is driven through a small `asyncio.run()` helper rather than adding a
plugin for one module.

Covered: extension matching including AppleDouble `._` sidecars and the
`allow_images` toggle, the MB-vs-KB size thresholds, the `_opt.`/`_trim.`
bypass, exclusion resolution (`~` expansion, `.` normalisation), subtree
pruning, scan-time persistence round-trip and its corrupt-file fallback,
multiple and missing scan targets, and the `(path, dir_changed)` tuple protocol
that `scanner/manager.py:249` unpacks.

Two behaviours worth naming. Excluding `.../skip` correctly leaves a sibling
`.../skipper` alone — the walk compares with `startswith(ex + os.sep)`, not a
bare prefix, so the obvious bug is already absent. And on an incremental pass an
*unchanged* directory bypasses the size filter entirely: `if dir_changed and not
self._is_valid_size(...)` means an undersized file still reaches the consumer
with `dir_changed=False`. That looks deliberate — downstream reuses its cached
entry instead of re-probing — so it is pinned, not changed.

#### The bug: an abandoned scan stranded its executor thread

`scan_directories` starts the walk in an executor thread that blocks on a
bounded `Queue(maxsize=500)` for backpressure. Nothing told that thread when the
consumer stopped reading — and `ScannerManager` stops reading exactly this way,
breaking out of `async for ... in fs_scanner.scan_directories(...)` the moment
its stop event fires (`manager.py:250-251`).

Reproduced with three stop-early scans against 600 files in one long-lived event
loop:

```
round 0: consumed 3, executor threads alive: 1 ['asyncio_0']
round 1: consumed 3, executor threads alive: 2 ['asyncio_0', 'asyncio_1']
round 2: consumed 3, executor threads alive: 3 ['asyncio_0', 'asyncio_1', 'asyncio_2']
```

One lost thread per abandoned scan, held until the event loop dies — which in
the server is process lifetime. `ThreadPoolExecutor`'s default worker count is
bounded (`min(32, cpu+4)`), so enough stopped scans would leave `run_in_executor`
with no free worker and the scanner would simply stop starting. Each abandoned
walker also printed `❌ Error walking <path>:` with an empty message at loop
teardown, which is where this would first show up in a log.

**Reachability, stated honestly:** `ScannerManager.stop()` has no caller today,
so no user can trigger this yet. It is public API on the manager and the `break`
that consumes it is live code, so this is a trap set for whoever wires up a
stop-scan button, not an outage today. Fixed on that basis, not because users
are hitting it.

**Fix:** the consumer sets a `threading.Event` in a `finally` block — which runs
when the generator is closed — and the walker re-checks it while waiting on the
queue instead of blocking indefinitely, returning its thread on cancellation.
The completion sentinel is skipped on an abandoned scan, since that put would
block for exactly the same reason.

**Test:** the scenario is made deterministic by pinning the loop to a
single-worker executor, so an unreleased thread means the *next* scan can never
start. Before the fix that test times out after 15s; after it, the second scan
returns all 600 files in 0.23s. The reproduction above now shows one thread
across all three rounds, reused rather than accumulated, and the spurious error
messages are gone.

Suite after iteration: **358 passed** under 3.13, **351 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1142.

### Iteration 8 — Phase B: `scanner/media_probe.py` → 34 tests, two bugs fixed

`asyncio.create_subprocess_exec` is replaced with a fake process, so no ffprobe
binary and no media file are involved. Covered: command construction and the
`ffprobe_path` fallback, non-zero exit, malformed JSON, spawn failure, stream
selection by `codec_type`, byte→MB and bps→Mbps conversion with rounding,
frame-rate parsing in all four shapes, `level` as number or string, audio-only
and video-only files, and the missing-format-block defaults.

#### Bug 1 — a hung ffprobe was never killed

`_run_ffprobe` wraps `process.communicate()` in `asyncio.wait_for(..., 20.0)`.
That only cancels the *coroutine*; the spawned ffprobe keeps running. Every
probe that hit the timeout left a live process behind, holding its two pipes,
for the rest of the scan. On a library with a handful of unreadable files that
accumulates silently.

Fixed by killing and reaping the child on any failure path where it is still
running (`returncode is None`). The test drives a deliberately hanging fake
through a 50 ms timeout and asserts both `kill()` and `wait()`; a second test
asserts a normally-exited process is *not* killed again.

#### Bug 2 — one `"N/A"` field dropped the whole file from the library

`get_metadata` parsed numbers with bare `float()` / `int()`:

```python
size_mb = float(fmt.get("size", 0)) / (1024 * 1024)
duration = float(fmt.get("duration", 0))
width = int(video_stream.get("width", 0))
```

ffprobe writes `"N/A"` for values it cannot determine — `duration` on some
containers, `width`/`height` on odd streams. `float("N/A")` raises, the blanket
`except Exception` at the end of `get_metadata` swallows it, and the caller
receives `None`. For a media inventory tool that means **the video never appears
in the library at all**, with no error surfaced anywhere — the failure mode is
"my file isn't in the scanner", which is close to unfindable.

Fixed with `_as_float` / `_as_int` helpers: an undeterminable field zeroes
itself and the file still gets indexed with everything else intact. Six tests
covered this, all red before the change. `_as_int` also accepts `"1920.0"`.

Frame-rate parsing now goes through the same helper, which removed the two bare
`except:` clauses it previously needed — the only lint touched here; the file's
remaining whitespace warnings were left alone.

Both bugs are in the module every scanned file passes through, and neither is
reachable through the existing suite, which had no coverage of this module at
all before tonight.

Suite after iteration: **392 passed** under 3.13, **385 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1133, down from the 1156 baseline.

### Iteration 9 — Phase B: `scanner/video_inspector.py` → 33 tests, no bugs

A 65-line adapter turning the legacy `VideoEntry` from `MediaProbe` into the
`MediaAsset` the scanner stores. It sits on the live scan path
(`manager.py:196`), so the mapping is pinned field by field with a stubbed
probe.

Covered: extension dispatch including case-insensitivity and the
"`.mp4` must be a suffix, not a substring" case, `None` passthrough when the
probe fails, every `VideoMetadata` field, user state (favorite, vaulted, tags,
thumb), and the flattened legacy properties `manager.py:211` reads straight off
the asset.

**Nothing was broken here** — three suspicions checked and dismissed rather than
assumed:

*The `or` idiom.* Every field is mapped as `legacy_entry.x or <default>`, which
normally silently converts legitimate falsy values. Harmless in this case: each
default equals the falsy value it would replace (`0` for ints, `0.0` for floats),
so only empty strings change, and `"" → "unknown"` is the intent. Pinned by
tests either way, including `None` for the `Optional` fields.

*`imported_at`.* `MediaAsset` defaults it to the current time, but the adapter
forwards the legacy value explicitly, so a fresh probe produces `0` and the
default factory never fires. That looked like a broken "Recent Imports"
collection — it is not. `manager.py:222` fills the timestamp in only when it is
still `0`, which is exactly what preserves the original import date across a
re-scan. Deliberate, and now documented by a test.

*Data loss into storage.* `sqlite_store._asset_to_video_entry` carries all
twelve `VideoMetadata` fields across, including `profile`, `level` and
`pixel_format`. No gap.

**One forward-compatibility note.** `MediaAsset.dict()` overrides Pydantic's
`dict()`, which is deprecated since V2 and removed in V3, to inject the
flattened frontend fields. Nothing in production calls it: every serialisation
path uses `model_dump(by_alias=True)`, which does *not* run the override, and
the store converts assets to `VideoEntry` before anything is dumped. So it is
dead code today rather than a bug — but if someone ever routes an asset through
it expecting the flat shape, `model_dump` will quietly return a different one.
Worth deleting during a Pydantic upgrade.

Suite after iteration: **425 passed** under 3.13, **418 passed / 7 skipped**
under 3.14. Repo-wide ruff: 1133.
